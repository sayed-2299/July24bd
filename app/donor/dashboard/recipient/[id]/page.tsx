import { useRouter } from 'next/navigation';

const RecipientPage = () => {
  const router = useRouter();

  return (
    <div>
      {/* Existing code */}
    </div>
  );
};

export default RecipientPage; 